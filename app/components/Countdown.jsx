var React = require('react');

var Countdown = React.createClass({
  render: function() {
    return(
      <div>
        <h2 className="page-title">Countdown.jsx</h2>
        <p>Component for Countdown.jsx file.</p>
      </div>
    );
  }
});

module.exports = Countdown;
