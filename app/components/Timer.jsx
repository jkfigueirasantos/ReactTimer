var React = require('react');

var Timer = React.createClass({
  render: function() {
    return(
      <div>
        <h2 className="page-title">Timer.jsx</h2>
        <p>Component for Timer.jsx file.</p>
      </div>
    );
  }
});

module.exports = Timer;
